apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nexus-push-credentials
  namespace: ${{ values.namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-secret-store
  target:
    name: nexus-push-credentials
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: |
          {"auths":{"{{ .registry }}":{"username":"{{ .username }}","password":"{{ .token }}","auth":"{{ printf "%s:%s" .username .token | b64enc }}"}}}
  data:
    - secretKey: username
      remoteRef:
        key: platform/nexus-push-credentials
        property: username
    - secretKey: token
      remoteRef:
        key: platform/nexus-push-credentials
        property: token
    - secretKey: registry
      remoteRef:
        key: platform/nexus-push-credentials
        property: registry
