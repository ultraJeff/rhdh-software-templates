apiVersion: pipelinesascode.tekton.dev/v1alpha1
kind: Repository
metadata:
  name: ${{ values.name }}
  namespace: ${{ values.namespace }}
spec:
  url: https://${{ values.ghHost }}/${{ values.ghOwner }}/${{ values.repoName }}
